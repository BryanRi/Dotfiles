#!/bin/bash
#
# Script to disable the xdg-screensaver

# the hex value of the root window id
# window_id= "xwininfo -root | awk '$1 == "xwininfo:" {print $4}'"

# disable the screensaver
xdg-screensaver suspend 0x7c8 # $window_id
